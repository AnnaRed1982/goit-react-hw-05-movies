import { useEffect } from 'react';

const Movies = () => {
  useEffect(() => {
    //http request
  }, []);

  return (
    <form>
      <input />
      <button type="submit">Search</button>
    </form>
  );
};

export default Movies;
